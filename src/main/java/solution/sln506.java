package solution;

import java.util.Arrays;

/**
 * @author kk
 * @description 相对名次
 * @date 2024-10-30 17:01:31
 */
public class sln506 {
    public String[] findRelativeRanks(int[] score) {
        String[] ans = new String[score.length];
        int[] temp = Arrays.copyOf(score,score.length);
        Arrays.sort(temp);
        for(int i = 0; i < score.length;i++){

        }
        return ans;
    }
}
